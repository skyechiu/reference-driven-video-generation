# Pipeline API Keys
# Fill in your actual keys below

OPENAI_API_KEY=
KLING_API_KEY=

# Optional: legacy character-reference folder for the /media/characters/ route
# (only used when no look package is selected). Defaults to assets/character_look
# if unset.
IP_CHARACTER_DIR=
